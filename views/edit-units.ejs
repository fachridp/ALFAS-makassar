<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- My CSS -->
    <link rel="stylesheet" href="css/add-products/add-products.css" />

    <!-- Bootstrap 5 CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@200;300;400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <script type="text/javascript">
      $(document).ready(function() {
        let input = $("#unit");
        let len = input.val().length;
        input[0].focus();
        input[0].setSelectionRange(len, len);
      });
    </script>

    <title><%= title %></title>
  </head>
  <body>
    <div class="container mt-3">
      <div class="row">
        <div class="col-md-6">
          <h2>Ubah Satuan Barang</h2>
          <% if (msg.length !== 0) { %>
            <div class="alert alert-success d-block mt-3" role="alert">
              <p id="check"> <%= msg %> </p>
            </div>
          <% } %>
          <form method="POST" action="/units?_method=PUT" autocomplete="off">
            <!-- Ambil _id -->
            <input type="hidden" name="_id" value="<%= units._id %>"/>
            <div class="mb-3">
              <label for="unit" class="form-label">Nama Satuan Barang :</label>
              <input
                type="text"
                class="form-control"
                id="unit"
                name="unit"
                value="<%= units.unit %>"
                required
                autofocus
              />
            </div>
            <a href="/units" class="btn btn-danger">Kembali</a>
            <button type="submit" class="btn btn-success">
              Simpan
            </button>
          </form>
        </div>
  </div>
</div>
  </body>
</html>
